# DeepSeek Harness macOS — Runtime Manager / 一键环境与版本管理开发规格

> 适用仓库：`sundusk/HarnessDesktop`  
> 目标平台：macOS  
> 开发方式：在现有 Phase 0–7 基础上增量开发，不重写已稳定模块。  
> 本文是可直接交给 DeepSeek Harness / Codex 执行的功能与架构规格。

---

## 0. 任务目标

在保持现有 **原生 macOS + Attach First + Web Core + Native Enhancement** 架构优势的前提下，补齐普通用户的“开箱即用”体验：

1. App 启动后自动检查 Harness 运行状态、版本和运行环境。
2. 已有 Harness 正在运行时，继续直接 Attach，绝不接管。
3. 没有运行 Harness 时，允许用户一键启动由 App 管理的 Harness。
4. 普通用户没有 Node / npx 时，允许一键准备 App 私有运行时，不修改系统 Node、Homebrew、Shell 配置或 PATH。
5. 只允许停止 App 自己启动的 Harness，绝不杀死 Terminal / 其他工具启动的 Harness。
6. 支持 Harness 本体版本检测：
   - 启动时静默检查；
   - 菜单栏手动“检查 Harness 更新”；
   - 当前版本 / 最新版本展示；
   - 用户明确确认后更新；
   - 保留上一版本并支持回退。
7. 继续保持 Harness Web UI 为核心兼容层；Native API / Runtime Manager 失败不得导致 Web UI 不可用。
8. 不增加 Profile Manager，不增加插件市场，不直接修改 `~/.dsh`、Profile、插件列表、Settings。

最终产品定位：

> **DeepSeek Harness Native macOS Client + Environment Doctor + Safe Launcher + Version Manager**

而不是：

> Harness Distribution / Profile Manager / Plugin Manager。

---

# 1. 现有能力必须保留

本任务必须基于当前工程继续开发，以下能力视为稳定基础，不允许为了 Runtime Manager 大规模重写：

- SwiftUI / AppKit 原生 macOS App。
- `WKWebView` 直接加载 Harness 官方 Web UI。
- Loopback-only Harness Endpoint。
- Harness Discovery。
- Menu Bar。
- Settings。
- `host.describe` Native Handshake。
- HTTP Transport / Compatibility Resolver。
- WebSocket Event Layer。
- ActivityReducer。
- 原生通知。
- Floating MoodBall。
- Native API 失败时 Web Core 继续工作的 degraded mode。

任何新功能都必须遵守“新增模块、最小侵入、可独立降级”的原则。

---

# 2. 架构原则升级

现有原则：

1. Attach First
2. Zero Mutation
3. Web Core + Native Enhancement

升级为以下四条：

## 2.1 Attach First

**任何时候都优先发现并连接已经运行的 Harness。**

App 启动后的第一优先级：

```text
探测 loopback Harness
        │
        ├─ 已存在 → Attach Existing
        │            不启动第二个 Harness
        │            不接管进程
        │            不停止进程
        │
        └─ 不存在 → 才进入 Managed Runtime 检查
```

禁止：

- 已发现 `127.0.0.1:<port>` 有有效 Harness 时再次启动 Managed Harness。
- 因版本不同而自动替换用户正在运行的 Harness。
- 因更新可用而自动停止外部 Harness。

---

## 2.2 Zero Configuration Mutation

重新定义 Zero Mutation：

> **Desktop 不直接修改 Harness 的配置资产。**

Desktop 永远禁止直接：

- 编辑 / 删除 / 重建 `~/.dsh`。
- 编辑 `settings.yaml`。
- 编辑 Profile manifest。
- 添加 / 删除 / 更新第三方插件。
- 修改 bundle 顺序。
- 修复插件依赖。
- 修改皮肤。
- 修改用户 Shell 配置。
- 修改 `.zshrc` / `.bashrc` / `.profile`。
- 修改系统 PATH。
- 执行 `brew install node`。
- 执行 `npm install -g ...`。
- 执行任意用户提供的 shell command。

允许：

- 读取运行中 Harness 通过公开 API 返回的信息。
- 在 App 自己的 Application Support 目录保存自己的状态。
- 管理 App 私有 Node Runtime。
- 管理 App 私有 npm cache。
- 启动官方 `@deepseek-ai/dsh`。
- 停止 App 自己启动的 Harness。
- 查询 npm registry 的版本元数据。

---

## 2.3 Explicit Runtime Management

所有会产生写入或进程变化的操作必须满足：

```text
用户明确操作
    ↓
明确目标
    ↓
明确作用范围
    ↓
只修改 App-owned 数据
```

允许的用户操作：

- 一键准备运行环境。
- 启动 Harness。
- 停止 Managed Harness。
- 更新 Managed Harness。
- 回退 Managed Harness。
- 删除 App 私有 Runtime / Cache（后期设置项，可选）。

禁止静默：

- 安装。
- 更新。
- 回退。
- 停止。
- 删除。

**自动检查可以静默，自动修改不可以。**

---

## 2.4 Web Core + Native Enhancement

继续保持：

```text
Native Runtime Manager 失败
        │
        ├─ 如果已有 Harness 可用 → Web UI 必须继续工作
        │
        └─ 如果没有 Harness → 显示可理解的启动/环境错误
```

Native API 兼容问题不得阻断 WKWebView。

---

# 3. 两种 Harness 所有权模型

必须引入明确的 Harness Ownership。

建议：

```swift
enum HarnessOwnership: Equatable, Sendable {
    case external
    case managed
}
```

## 3.1 External Harness

定义：

- 用户在 Terminal 启动；
- 其他 App 启动；
- LaunchAgent / 服务启动；
- 总之不是本次 HarnessDesktop Runtime Supervisor 创建。

行为：

```text
External Harness
├─ 可以 Attach
├─ 可以读取 host.describe.version
├─ 可以检查是否有新版
├─ 可以显示更新提示
├─ 可以断开 Desktop 自己的连接
│
├─ 禁止 Stop
├─ 禁止 Restart
├─ 禁止 Update
└─ 禁止 Rollback
```

核心原则：

> **Never kill what you do not own.**

---

## 3.2 Managed Harness

定义：

- 由 HarnessDesktop 的 Runtime Supervisor 创建。
- 进程身份与 generation token 被 App 记录。
- 使用 App 规定的 exact Harness version。
- 使用 App-controlled Node / npm cache。

行为：

```text
Managed Harness
├─ Start
├─ Stop
├─ Restart
├─ Update
├─ Rollback
├─ Inspect
└─ Attach
```

只允许管理“当前 generation 自己创建”的进程树。

---

# 4. 新增总体架构

推荐目标结构：

```text
┌────────────────────────────────────────────────────┐
│ DeepSeek Harness.app                               │
│                                                    │
│  SwiftUI / AppKit / WKWebView                      │
│                                                    │
│  ┌──────────────────┐                              │
│  │ AppCoordinator   │                              │
│  └────────┬─────────┘                              │
│           │                                        │
│  ┌────────▼─────────┐   ┌──────────────────────┐   │
│  │ HarnessDiscovery│   │ HarnessVersionService│   │
│  └────────┬─────────┘   └──────────┬───────────┘   │
│           │                        │               │
│  ┌────────▼─────────┐   ┌──────────▼───────────┐   │
│  │EnvironmentDoctor│   │ RuntimeManagerClient │   │
│  └────────┬─────────┘   └──────────┬───────────┘   │
└───────────┼────────────────────────┼───────────────┘
            │                        │ XPC / IPC
            │ HTTP / WS              │
            ▼                        ▼
     Existing Harness       ┌────────────────────────┐
                            │ Harness Runtime Helper │
                            │ user-level / signed    │
                            │ narrow API only        │
                            └───────────┬────────────┘
                                        │
                               App-owned Node Runtime
                                        │
                                        ▼
                       @deepseek-ai/dsh@<exact-version>
                                        │
                                        ▼
                              Managed Harness Host
```

---

# 5. 为什么主 App 不能直接启动 Harness

当前主 App 使用 App Sandbox。

如果由 Sandbox App 直接创建 Node / Harness 子进程，子进程会继承 Sandbox 约束。

DeepSeek Harness 是 Coding Agent / Agent Harness，需要访问用户选择的工作目录、代码仓库、工具链和文件系统；如果它继承主 App 的极窄 Sandbox，将导致 Harness 核心能力被错误限制。

因此：

> **不要在 SwiftUI 主进程里直接 `Process()` 启动 Harness。**

推荐使用一个独立的、签名的、用户级 Runtime Helper / LaunchAgent，通过 XPC 或受控 IPC 与主 App 通信。

优先考虑：

- macOS `ServiceManagement` / `SMAppService` 用户级 Agent。
- Helper 与主 App 同一 Developer ID 签名体系。
- Helper 不提供任意 shell 接口。
- Helper API 必须强类型白名单。

如果实现阶段发现 Sandbox + Helper 的签名 / IPC 成本不可接受，允许 V1 采用 Developer ID 站外分发、主 App 非 Sandbox 的简化方案，但必须：

1. 在 `ARCHITECTURE.md` 记录 ADR；
2. 保留所有命令白名单限制；
3. 不得因为取消 Sandbox 就增加任意 shell 能力。

---

# 6. Runtime Helper API

禁止设计：

```swift
runCommand(_ command: String)
runShell(_ script: String)
execute(arguments: [String])
```

必须设计为强类型能力：

```swift
protocol HarnessRuntimeManaging {
    func inspectRuntime() async throws -> RuntimeInspection
    func prepareRuntime() async throws -> RuntimePreparationResult

    func startHarness(
        version: String,
        port: Int,
        dataMode: ManagedDataMode
    ) async throws -> ManagedHarnessIdentity

    func stopHarness(
        identity: ManagedHarnessIdentity
    ) async throws

    func status(
        identity: ManagedHarnessIdentity
    ) async throws -> ManagedHarnessProcessStatus
}
```

可后续增加：

```swift
func clearRuntimeCache() async throws
func runtimeDiskUsage() async throws -> Int64
```

禁止从 UI 向 Helper 传入：

- arbitrary executable path；
- arbitrary shell；
- arbitrary environment dictionary；
- arbitrary command line。

---

# 7. App 私有 Runtime

## 7.1 原则

Managed Mode 不依赖用户是否安装：

- Homebrew；
- nvm；
- fnm；
- Volta；
- asdf；
- Node；
- npm；
- npx。

推荐 Managed Mode 使用 **App-owned Node Runtime**，而不是默认依赖系统 Node。

原因：

1. 普通用户真正可以一键使用。
2. 不修改系统 Node。
3. 不依赖 Finder 与 Terminal PATH 差异。
4. 不依赖 `.zshrc`。
5. Node 版本可控。
6. npm cache 可隔离。
7. 系统已有开发环境不受影响。

---

## 7.2 Runtime 目录

建议：

```text
~/Library/Application Support/
  dev.deepseekharness.DeepSeekHarness/
    Runtime/
      node/
      npm-cache/
      state/
        managed-runtime.json
```

如果 App Sandbox container 路径不同，以系统实际 container 为准。

禁止写：

```text
/usr/local
/opt/homebrew
~/.npm
~/.node
~/.nvm
~/.volta
```

Managed npm 必须设置自己的 cache：

```text
npm_config_cache=<AppSupport>/Runtime/npm-cache
```

不得修改全局 npm config。

---

## 7.3 Node Runtime 策略

优先方案：

- Release 包内携带兼容的 Node Runtime；
- Apple Silicon / Intel 分架构处理；
- Node Runtime 属于 Desktop 自己的基础设施；
- Harness 本体不随 App 固定打包。

不要把某个固定 Harness source/submodule vendoring 进 App。

Environment Doctor 必须校验当前 Runtime Node 是否满足目标 Harness package 的 engine 要求。

如果不满足：

- 不自动下载不可信二进制；
- 显示“运行环境需要更新”；
- 后续可通过 HarnessDesktop 自身升级携带新的 Node Runtime。

---

# 8. Harness 数据隔离策略

必须区分 Existing 与 Managed。

## 8.1 Existing Harness

External Harness 已运行：

```text
Desktop
    ↓ Attach
External Harness
    ↓
用户原有 DSH_HOME / ~/.dsh
```

Desktop 不读写其数据。

---

## 8.2 Managed Harness

对于“从未配置 Harness”的普通用户，推荐默认使用隔离数据目录：

```text
~/Library/Application Support/
  dev.deepseekharness.DeepSeekHarness/
    ManagedHarnessHome/
```

Helper 启动 Harness 时只对子进程设置：

```text
DSH_HOME=<ManagedHarnessHome>
```

意义：

- 第一次使用不会污染 `~/.dsh`。
- 不会与 Terminal Harness 插件 / 皮肤 / Profile 冲突。
- Harness 自己仍然拥有该目录的数据语义。
- Desktop 不直接编辑里面的内容。

注意：

> Desktop 可以创建 ManagedHarnessHome 目录，但不得理解或修改其内部 Harness 数据结构。

后续可以增加高级设置：

```text
Harness 数据模式

● 隔离的 Managed Harness 数据（推荐）
○ 使用默认 Harness 数据目录（高级）
```

V1 建议只实现隔离模式，降低风险。

---

# 9. Environment Doctor

新增：

```text
Harness/Runtime/EnvironmentDoctor
```

建议模型：

```swift
struct HarnessEnvironmentReport: Equatable, Sendable {
    var discoveredEndpoint: HarnessEndpoint?
    var ownership: HarnessOwnership?
    var runningVersion: HarnessVersion?

    var managedRuntime: ManagedRuntimeStatus
    var managedVersion: HarnessVersion?
    var latestVersion: HarnessVersion?

    var updateStatus: HarnessUpdateStatus
}
```

启动检查顺序必须固定：

```text
1. Probe configured loopback endpoint
2. 如果 Harness 正在运行
   2.1 host.describe
   2.2 获得 running version
   2.3 标记 external / managed
   2.4 直接进入 Attach
3. 后台查询 latest version
4. 如果没有 Harness
   4.1 检查 Managed Runtime
   4.2 检查 managedVersion
   4.3 决定 UI：Start / Prepare
```

Environment Doctor 的“检查”必须只读。

---

# 10. Harness 当前版本检测

正在运行的 Harness：

```text
POST /api/host.describe
```

使用现有 Native Handshake 返回的：

```text
version
```

作为 **Running Harness Version 的唯一优先来源**。

禁止为了获取 running version：

- 扫 npm 全局包；
- 扫 `~/.npm`；
- 解析进程 argv；
- 读取 `~/.dsh`；
- 执行 shell。

如果 `host.describe` 不可用：

```text
runningVersion = unknown
```

Web UI 继续工作。

---

# 11. Harness 最新版本检测

新增：

```text
Harness/Runtime/HarnessVersionService
```

职责：

- 查询 npm registry；
- 获取 `@deepseek-ai/dsh` 的 `dist-tags.latest`；
- 解析 semantic/prerelease version；
- 计算 update state；
- 做缓存和请求节流。

推荐通过 `URLSession` 直接访问 npm registry，而不是执行：

```text
npm view ...
```

原因：

- 查询版本不需要 shell。
- 主 App 可直接做 HTTPS。
- 更符合 Zero Configuration Mutation。
- 更容易测试。

版本状态：

```swift
enum HarnessUpdateStatus: Equatable, Sendable {
    case unknown
    case checking
    case upToDate(current: HarnessVersion)
    case updateAvailable(current: HarnessVersion, latest: HarnessVersion)
    case aheadOfLatest(current: HarnessVersion, latest: HarnessVersion)
    case failed
}
```

必须正确处理 prerelease，例如：

```text
0.1.0-rc.7
0.1.0-rc.8
```

不要使用简单字符串比较。

---

# 12. 更新检查策略

## 12.1 App 启动

启动时：

- 静默检查；
- 不弹 modal；
- 不自动更新；
- 网络失败不打扰；
- 最多改变菜单栏状态。

建议缓存：

```text
lastUpdateCheckDate
latestKnownHarnessVersion
```

默认节流：

```text
启动时如果距离上次成功检查 < 6 小时
→ 使用缓存
```

手动“检查 Harness 更新”忽略节流。

---

## 12.2 菜单栏手动检查

必须增加：

```text
检查 Harness 更新…
```

点击后：

```text
checking
   ↓
upToDate / updateAvailable / failed
```

手动检查失败时允许显示轻量错误提示。

---

# 13. Managed Harness 版本固定

**Managed Harness 禁止每次启动直接使用 `@latest`。**

错误：

```bash
npx @deepseek-ai/dsh@latest web
```

正确逻辑：

```text
latest = registry.dist-tags.latest
        ↓
第一次 Prepare 时确认 exact version
        ↓
managedVersion = 0.1.0-rc.7
        ↓
以后始终启动 exact version
```

概念命令：

```bash
npx --yes @deepseek-ai/dsh@0.1.0-rc.7 web
```

或等价的 exact-version 官方入口。

必须把：

```text
managedVersion
previousManagedVersion
```

保存在 HarnessDesktop 自己的设置中。

禁止写进 Harness Profile。

---

# 14. 一键准备运行环境

当：

```text
Harness 未运行
+
Managed Runtime 未准备
```

UI：

```text
DeepSeek Harness 尚未准备好

HarnessDesktop 可以为你准备隔离的运行环境。
不会修改系统 Node、Homebrew、Shell 或现有 Harness 配置。

[一键准备]
```

执行：

```text
1. 校验 App-owned Node Runtime
2. 查询 latest Harness version
3. 保存 exact managedVersion
4. 准备 private npm cache
5. 预热 / 拉取 exact Harness package
6. 验证 package 可执行
7. 不自动启动，或准备完成后按当前 UX 直接进入 Start
```

推荐“准备完成后自动启动”，但必须是由用户本次点击“一键准备”明确触发。

---

# 15. Harness package 获取

Harness 本体不全局安装。

禁止：

```bash
npm install -g @deepseek-ai/dsh
```

禁止：

```bash
brew ...
```

推荐使用官方 npm package 的 exact version，并把 npm cache 定向到 App-owned 目录。

Helper 启动 Managed Harness 时需要使用：

```text
App-owned Node/npm/npx
+
App-owned npm cache
+
exact @deepseek-ai/dsh version
+
child-scoped DSH_HOME
```

不得修改父 Shell 环境。

---

# 16. 一键启动

状态：

```swift
enum HarnessRuntimeState: Equatable, Sendable {
    case checking
    case notRunning
    case preparing
    case starting(version: HarnessVersion)
    case runningExternal(version: HarnessVersion?)
    case runningManaged(version: HarnessVersion?)
    case stopping
    case failed(HarnessRuntimeFailure)
}
```

启动流程：

```text
用户点击 Start
    ↓
重新 Probe endpoint
    ↓
如果已有 Harness
    → Abort Start
    → Attach Existing
    ↓
否则
    ↓
检查 Runtime
    ↓
获取 managedVersion
    ↓
Helper startHarness()
    ↓
记录 ManagedHarnessIdentity
    ↓
等待 loopback ready
    ↓
host.describe
    ↓
确认版本
    ↓
Attach
```

必须防止：

- 双击 Start 启动两个进程；
- Start 与 Update 并发；
- Start 与 Stop 并发；
- endpoint 已被占用仍启动。

---

# 17. ManagedHarnessIdentity

不要只存 PID。

建议：

```swift
struct ManagedHarnessIdentity: Codable, Equatable, Sendable {
    let generationID: UUID
    let pid: Int32
    let startedAt: Date
    let version: String
    let port: Int
}
```

Helper 还应维护自己的活跃 generation。

Stop 时必须同时验证：

```text
generationID
+
pid
+
helper-owned process reference
```

避免 PID reuse 导致误杀其他进程。

---

# 18. 一键停止

只对 Managed Harness显示：

```text
停止 Harness
```

External Harness：

```text
断开连接
```

不显示 Stop，或者置灰并明确：

```text
此 Harness 由外部启动，HarnessDesktop 不会停止它
```

停止流程：

```text
SIGINT / graceful request
    ↓
等待退出
    ↓ timeout
SIGTERM
    ↓
等待退出
    ↓
仅在确定仍为 owned generation 时执行最后兜底
```

不要默认 `kill -9`。

必须管理完整 process tree，避免：

```text
npx 退出
node child 仍存活
```

---

# 19. App 退出策略

设置新增：

```text
退出 HarnessDesktop 时：
● 保持 Managed Harness 运行
○ 同时停止 Managed Harness
```

V1 推荐默认：

```text
同时停止 Managed Harness
```

原因：

- Managed Harness 是 App 启动的 foreground runtime；
- 避免后台遗留。

但用户必须可以修改。

如果是 External Harness：

```text
App 退出永远不影响 External Harness
```

---

# 20. Harness 更新 UX

当：

```text
current < latest
```

启动时不弹窗。

菜单栏显示：

```text
Harness
当前：0.1.0-rc.7
最新：0.1.0-rc.8
⬆ 有更新可用
```

External Harness：

```text
[查看更新方式]
```

Managed Harness：

```text
[更新 Harness…]
```

---

# 21. Managed Harness 更新

点击：

```text
更新 Harness…
```

必须显示确认界面：

```text
DeepSeek Harness 有新版本

当前：0.1.0-rc.7
最新：0.1.0-rc.8

DeepSeek Harness 仍处于快速迭代阶段，
新版本可能影响第三方插件或 Native API 兼容。

[取消] [更新并重新启动]
```

更新流程：

```text
1. latest version 已重新从 registry 验证
2. previousManagedVersion = managedVersion
3. 准备 latest exact package
4. 如果当前 Managed Harness 正在运行：
      graceful stop
5. managedVersion = latest
6. start exact latest
7. 等待 loopback
8. host.describe
9. 验证 reported version
10. Attach
```

重要：

> 在新版本成功启动并通过 `host.describe` 之前，不要删除 previous version/cache。

---

# 22. 回退版本

当存在：

```text
previousManagedVersion != nil
```

设置或菜单可以显示：

```text
回退到 0.1.0-rc.7…
```

确认：

```text
当前：0.1.0-rc.8
回退：0.1.0-rc.7

[取消] [回退并重新启动]
```

流程：

```text
Stop managed
    ↓
swap managedVersion / previousManagedVersion
    ↓
Start exact previous
    ↓
host.describe verify
```

如果回退启动失败：

- 不删除当前版本记录；
- 标记 failure；
- 提供“恢复到 rc.8”操作。

---

# 23. 更新失败的事务语义

Update 必须尽量事务化。

禁止：

```text
先覆盖 current version
→ 再下载
→ 下载失败
→ 用户没有可运行版本
```

正确：

```text
PrepareCandidate(latest)
    ↓ success
StopCurrent
    ↓
LaunchCandidate
    ↓ health check success
Commit managedVersion
```

如果 candidate 启动失败：

```text
Candidate failed
    ↓
尝试恢复 previous working version
    ↓
恢复成功 → Attach previous
恢复失败 → 显示明确错误
```

---

# 24. 菜单栏设计

## 24.1 External Harness Running

```text
DeepSeek Harness

● 工作中 / 空闲 / 等待…
Harness：已连接
来源：外部
版本：0.1.0-rc.7

最新：0.1.0-rc.8
⬆ 有更新可用

检查 Harness 更新…
查看更新方式…

────────────
显示主窗口
重新加载
在浏览器打开
显示心情球 ✓
设置…
退出
```

不出现：

```text
停止 Harness
更新并重启
回退
```

---

## 24.2 Managed Harness Running

```text
DeepSeek Harness

● 工作中
Harness：已连接
来源：HarnessDesktop
版本：0.1.0-rc.7

⬆ 0.1.0-rc.8 可用

更新 Harness…
停止 Harness
检查 Harness 更新…

────────────
...
```

---

## 24.3 Harness Not Running + Runtime Ready

```text
DeepSeek Harness

○ Harness 未运行

Managed Runtime：已就绪
Harness：0.1.0-rc.7

启动 Harness
检查 Harness 更新…

────────────
...
```

---

## 24.4 First Run / Runtime Missing

```text
DeepSeek Harness

○ Harness 未配置

Managed Runtime：未准备

一键准备运行环境…
检查 Harness 更新…

────────────
...
```

---

# 25. 主窗口未运行页升级

现有未运行页从：

```text
复制启动命令
重新检测
```

升级为状态驱动。

## External-capable / Advanced User

仍保留：

```text
复制终端启动命令
```

## Managed Runtime Ready

增加主按钮：

```text
[启动 Harness]
```

## Runtime Missing

增加：

```text
[一键准备]
```

说明必须明确：

> 不会修改系统 Node、Homebrew、Shell 配置或已有 Harness 数据。

---

# 26. Settings 新增

建议新增“运行环境”标签页。

内容：

```text
运行环境

Harness 模式
  当前：External / Managed / 未运行

Managed Harness
  当前版本：0.1.0-rc.7
  上一版本：0.1.0-rc.6
  最新版本：0.1.0-rc.8

[检查更新]
[更新]
[回退]

启动
☑ 打开 HarnessDesktop 时自动启动 Managed Harness

退出
● 退出 App 时停止 Managed Harness
○ 保持 Managed Harness 运行

数据
Managed Harness 数据：隔离模式
路径：[显示路径]
```

V1 不提供 Profile 修改。

---

# 27. 自动启动

设置：

```text
launchManagedHarnessAtAppStart
```

默认：

```text
false
```

只有以下条件全部满足才自动启动：

```text
用户明确开启
+
没有发现 External Harness
+
Managed Runtime ready
+
managedVersion 有效
```

如果发现 External Harness：

```text
忽略 auto-start
直接 Attach Existing
```

---

# 28. 更新通知

不要每次有更新就弹系统通知。

默认：

- 菜单栏标记；
- Settings 显示；
- 主窗口状态页可提示。

可选后续设置：

```text
Harness 新版本通知
```

但 V1 不需要。

---

# 29. 并发控制

Runtime Manager 必须有单一 mutation gate。

建议：

```swift
actor HarnessRuntimeController
```

保证以下操作互斥：

- prepare
- start
- stop
- update
- rollback

读取操作可以并发：

- status
- version check
- environment inspect

但 version check 需要 single-flight，避免多请求。

---

# 30. 错误模型

不要把底层 stderr 直接显示给普通用户。

建议：

```swift
enum HarnessRuntimeFailure: Error, Equatable {
    case helperUnavailable
    case runtimeMissing
    case runtimeIncompatible
    case endpointOccupied
    case packagePreparationFailed
    case startFailed
    case startupTimeout
    case versionVerificationFailed
    case stopFailed
    case updateFailed
    case rollbackFailed
    case networkUnavailable
}
```

UI：

```text
无法启动 DeepSeek Harness

运行环境已准备，但 Harness 没有成功启动。

[重试] [查看诊断信息]
```

诊断页可以显示：

- error code；
- phase；
- exit code；
- 非敏感 stderr 尾部；
- runtime version；
- expected Harness version；
- reported Harness version。

禁止记录：

- Prompt；
- Conversation；
- API Key；
- Cookie；
- Token；
- 用户代码内容。

---

# 31. 日志

新增 OSLog category：

```text
runtime.environment
runtime.helper
runtime.process
runtime.version
runtime.update
runtime.rollback
```

日志必须做到：

- 不记录完整环境变量。
- 不记录 credentials。
- 不记录 npm auth。
- 不记录 DSH secrets。
- 不记录用户工作目录内容。
- PID / version / port 可以记录。
- managed data path 建议隐去用户名或使用 privacy。

---

# 32. 安全要求

## 必须

- Helper 只接受签名主 App 的连接。
- XPC interface 强类型。
- 禁止 arbitrary command。
- 禁止 shell script 字符串。
- 所有路径 canonicalize。
- App-owned Runtime 必须限制在固定根目录。
- Managed DSH_HOME 必须限制在固定根目录。
- Start 前重新检查 endpoint。
- Stop 前验证 generation ownership。
- Update / rollback 前重新确认 ownership。
- 网络下载必须 HTTPS。
- npm metadata 不可信字段必须安全解析。
- 版本字符串必须有长度限制。
- stderr / stdout 必须做 bounded buffer，防止内存无限增长。

---

# 33. 兼容性要求

DeepSeek Harness 处于快速迭代期，因此：

1. `host.describe` 解码继续宽松。
2. version unknown 不允许 crash。
3. Native API 不支持时：
   - Web UI 继续；
   - Version 显示 Unknown；
   - Runtime Update 仍可基于 Managed metadata 工作。
4. 不硬编码“当前最新 Harness 版本”。
5. 不硬编码永远固定的 Node engine。
6. 版本服务与运行时服务分层。

---

# 34. 测试策略

所有新逻辑必须协议化、可注入。

禁止在 Unit Test 真正启动 npm / Node / Harness。

建议 protocols：

```swift
protocol HarnessVersionProviding
protocol HarnessEnvironmentInspecting
protocol HarnessRuntimeManaging
protocol HarnessProcessStatusProviding
protocol RuntimeClock
```

---

# 35. Unit Tests

至少覆盖：

## Version

- stable semver compare。
- prerelease compare。
- rc.7 < rc.8。
- current == latest。
- current > latest。
- invalid version。
- registry malformed。
- registry timeout。
- cache throttling。
- manual check bypass cache。

## Ownership

- discovered unknown process → external。
- helper generation match → managed。
- PID same but generation mismatch → external / invalid ownership。
- External 不允许 stop。
- External 不允许 update。

## Runtime State

- missing → prepare。
- ready → start。
- concurrent start dedupe。
- start 时 endpoint 突然出现 → attach existing。
- start timeout。
- stop success。
- graceful stop timeout fallback。
- process exited unexpectedly。

## Update

- update success。
- candidate prepare failed → current 不变。
- candidate launch failed → previous 恢复。
- version verify mismatch → update 不 commit。
- rollback success。
- rollback failure。
- update 与 stop 不并发。

## Security

- arbitrary path rejected。
- path traversal rejected。
- unknown command impossible。
- wrong generation cannot stop process。
- oversized stdout/stderr bounded。

---

# 36. Integration / Smoke Tests

需要新增本地 smoke scripts 或测试 target。

至少：

### Smoke A — Existing Harness

```text
Terminal 启动 Harness
→ 打开 App
→ 自动 Attach
→ ownership = external
→ Stop 不可用
→ 退出 App
→ Terminal Harness 仍运行
```

### Smoke B — Managed Start

```text
确保无 Harness
→ Prepare
→ Start
→ 3080 ready
→ host.describe
→ ownership = managed
→ Stop
→ Harness 正常退出
```

### Smoke C — Collision

```text
App 准备 Start
→ 在 Start 前另一进程占用 port
→ App 不启动第二份
→ Attach Existing
```

### Smoke D — Update

```text
Managed vA
→ mock latest vB
→ update
→ vB health check success
→ managedVersion = vB
→ previous = vA
```

### Smoke E — Failed Update

```text
Managed vA
→ candidate vB fail
→ restore vA
→ App 可继续使用
```

### Smoke F — Zero Configuration Mutation

对比操作前后：

```text
~/.dsh
Shell rc
Homebrew
system PATH
global npm packages
```

HarnessDesktop 不得直接修改。

---

# 37. UI 验收标准

## 首次用户

目标：

```text
下载 App
→ 打开
→ 一键准备
→ 自动启动
→ 进入 Harness
```

不要求用户：

- 打开 Terminal。
- 安装 Homebrew。
- 安装 Node。
- 输入 npm command。
- 理解 Profile。

---

## 现有 Harness 用户

目标：

```text
Terminal Harness 已运行
→ 打开 App
→ 直接连接
```

现有：

- Profile；
- 插件；
- 皮肤；
- Terminal 使用方式；

均不被 HarnessDesktop 改动。

---

# 38. 非目标

本阶段明确不开发：

- 插件市场。
- 插件安装 / 删除 / 更新。
- Profile Manager。
- Profile 自动修复。
- 修改 Harness settings。
- 自动迁移 `~/.dsh`。
- 自动复制用户插件到 Managed Home。
- 从 External Harness 强制切换到 Managed Harness。
- 静默更新 Harness。
- 自动回退。
- 任意终端。
- 内嵌 Shell。
- 通用命令执行器。
- Fork / vendoring DeepSeek Harness source。

---

# 39. 推荐目录

建议新增：

```text
DeepSeek Harness/
├── Harness/
│   ├── Discovery/
│   ├── Transport/
│   ├── Compatibility/
│   └── Runtime/
│       ├── EnvironmentDoctor.swift
│       ├── HarnessOwnership.swift
│       ├── HarnessRuntimeState.swift
│       ├── HarnessRuntimeController.swift
│       ├── HarnessVersionService.swift
│       ├── HarnessUpdateStatus.swift
│       ├── ManagedHarnessIdentity.swift
│       └── RuntimeManagerClient.swift
│
├── RuntimeHelper/
│   ├── RuntimeHelperProtocol.swift
│   ├── RuntimeHelperService.swift
│   ├── ManagedProcessSupervisor.swift
│   ├── ManagedNodeRuntime.swift
│   └── ManagedPackageCache.swift
│
├── Desktop/
│   ├── Runtime/
│   │   ├── RuntimeStatusView.swift
│   │   └── HarnessUpdateView.swift
│   └── Settings/
│       └── RuntimeSettingsView.swift
│
└── Infrastructure/
    ├── RuntimeSettings.swift
    └── RuntimeLogging.swift
```

实际目录可按现有工程风格微调，但职责边界必须保留。

---

# 40. AppCoordinator 接入

AppCoordinator 不直接执行 Runtime 命令。

建议：

```text
AppCoordinator
   │
   ├─ Discovery
   ├─ Adapter
   ├─ ActivityReducer
   ├─ Notification
   ├─ MoodBall
   │
   └─ HarnessRuntimeController
          │
          ├─ EnvironmentDoctor
          ├─ VersionService
          └─ RuntimeManagerClient
```

AppCoordinator 只负责：

- 暴露 UI 状态；
- 响应用户 intent；
- 协调 Attach；
- 不持有低层 Process。

---

# 41. Phase 8 — Runtime Domain & Environment Doctor

目标：只读，不启动任何进程。

- [ ] 新增 `HarnessOwnership`
- [ ] 新增 `HarnessRuntimeState`
- [ ] 新增 `HarnessEnvironmentReport`
- [ ] 新增 `HarnessVersionService`
- [ ] npm registry latest 查询
- [ ] semver / prerelease compare
- [ ] 启动静默 update check
- [ ] 菜单栏 `检查 Harness 更新…`
- [ ] 当前 / 最新版本 UI
- [ ] `host.describe.version` 接入统一 Version Model
- [ ] Unit Tests
- [ ] Build
- [ ] Test

验收：

```text
已有 Harness → 正确显示当前版本
latest 查询成功 → 正确显示更新状态
网络失败 → 不影响现有 Harness
```

---

# 42. Phase 9 — Runtime Helper Skeleton

目标：建立安全进程边界，不启动 Harness。

- [ ] Runtime Helper target / Agent
- [ ] ServiceManagement 注册
- [ ] XPC / IPC
- [ ] Caller identity validation
- [ ] 强类型 API
- [ ] 禁止 arbitrary command
- [ ] Helper health check
- [ ] Unit Tests
- [ ] Build
- [ ] Test

验收：

```text
主 App 能查询 Helper 状态
Helper 没有 shell API
```

---

# 43. Phase 10 — App-owned Node Runtime

目标：普通用户不依赖系统 Node。

- [ ] Node Runtime packaging
- [ ] arm64
- [ ] x86_64
- [ ] Runtime version inspection
- [ ] private npm cache
- [ ] Managed Harness Home
- [ ] Runtime preparation UI
- [ ] 一键准备
- [ ] Zero system mutation smoke
- [ ] Build / Test

验收：

```text
一台没有 Homebrew / Node 的 Mac
→ App 可以准备 Runtime
→ 系统 PATH 不变
```

---

# 44. Phase 11 — Managed Start / Stop

- [ ] `ManagedProcessSupervisor`
- [ ] generation ownership
- [ ] exact Harness version
- [ ] endpoint collision protection
- [ ] one-click Start
- [ ] graceful Stop
- [ ] process tree cleanup
- [ ] unexpected exit handling
- [ ] auto-start setting
- [ ] App exit policy
- [ ] External Harness protection
- [ ] Unit + Smoke
- [ ] Build / Test

验收：

```text
External → 绝不 stop
Managed → 可 Start / Stop
```

---

# 45. Phase 12 — Update / Rollback

- [ ] managedVersion
- [ ] previousManagedVersion
- [ ] update confirmation
- [ ] candidate preparation
- [ ] health-check-before-commit
- [ ] failed update restore
- [ ] rollback
- [ ] version mismatch protection
- [ ] UI
- [ ] Tests
- [ ] Build

验收：

```text
更新不静默
更新失败不破坏上一版本
允许一键回退
```

---

# 46. Phase 13 — UX Polish

- [ ] Runtime Settings tab
- [ ] Menu Bar 状态精简
- [ ] 未运行页升级
- [ ] First Run onboarding
- [ ] Error diagnostics
- [ ] Accessibility labels
- [ ] 中文文案统一
- [ ] Intel / Apple Silicon smoke
- [ ] Offline UX
- [ ] Build / Test

---

# 47. Phase 14 — Release Hardening

- [ ] Helper signing
- [ ] Hardened Runtime
- [ ] Notarization
- [ ] Runtime binary integrity verification
- [ ] Update security review
- [ ] Zero Configuration Mutation audit
- [ ] External Harness protection audit
- [ ] Crash recovery
- [ ] stale managed identity cleanup
- [ ] full regression of Phase 0–7
- [ ] README update
- [ ] ARCHITECTURE update
- [ ] DEVELOPMENT update
- [ ] AGENTS constraints update

---

# 48. 文档同步要求

实现过程中必须同步：

## `ARCHITECTURE.md`

新增 ADR：

- ADR — Attach First remains authoritative
- ADR — External vs Managed ownership
- ADR — Zero Configuration Mutation
- ADR — Runtime Helper boundary
- ADR — App-owned Node Runtime
- ADR — Managed exact Harness version
- ADR — Update is explicit and transactional

## `DEVELOPMENT.md`

追加 Phase 8–14 状态。

不要删除 Phase 0–7 历史。

## `AGENTS.md`

增加永久约束：

1. Never stop External Harness.
2. Never expose arbitrary shell execution.
3. Never edit `~/.dsh` directly.
4. Never auto-update Harness.
5. Never start `@latest` for Managed Harness.
6. Managed Harness always uses exact version.
7. Runtime writes only to App-owned paths.
8. Native Runtime failure must not break Web Core.
9. Every process mutation requires ownership verification.

## `README.md`

最终面向用户说明：

- 已运行 Harness：自动连接。
- 未运行：可一键启动。
- 无 Node：可一键准备私有 Runtime。
- 不修改系统 Node / Shell / Homebrew。
- 不直接修改用户 Harness Profile / Plugin。
- 自动检查版本，但不自动更新。
- External Harness 不会被 App 停止。

---

# 49. AI Coding Agent 执行规则

交给 DeepSeek Harness / Codex 后，执行顺序必须是：

```text
1. 阅读：
   AGENTS.md
   ARCHITECTURE.md
   DEVELOPMENT.md
   README.md

2. 阅读当前 Runtime / Discovery / AppCoordinator / MenuBar 代码

3. 不重写 Phase 0–7

4. 从 Phase 8 开始

5. 每个 Phase：
   实现
   → Unit Test
   → xcodebuild build
   → xcodebuild test
   → 修 warning
   → 人工/自动 smoke
   → 更新 DEVELOPMENT.md

6. 一个 Phase 验收通过后再进入下一 Phase
```

不得一次性把 Phase 8–14 全部混在一个巨大修改中。

---

# 50. Definition of Done

本功能最终完成必须同时满足：

### 新用户

```text
安装 HarnessDesktop
→ 打开
→ 一键准备
→ 启动 Harness
→ 使用
```

### 高级用户

```text
Terminal 启动自己的 Harness
→ HarnessDesktop 自动 Attach
→ 原插件 / 皮肤 / Profile 不受影响
```

### 更新

```text
自动检测
→ 提示
→ 用户确认
→ 更新 exact version
→ health check
→ 成功 commit
```

### 回退

```text
新版本异常
→ 用户回退
→ 上一版本重新启动
```

### 安全

必须证明：

```text
HarnessDesktop 不直接修改 ~/.dsh
HarnessDesktop 不修改 Shell
HarnessDesktop 不修改系统 PATH
HarnessDesktop 不全局安装 Node
HarnessDesktop 不全局安装 DSH
HarnessDesktop 不停止 External Harness
HarnessDesktop 不提供 arbitrary shell
HarnessDesktop 不静默更新 Harness
```

### 架构

最终仍满足：

> **Harness is the server. DeepSeek Harness is a native macOS client.**

并扩展为：

> **Attach First. Zero Configuration Mutation. Explicit Runtime Management. Web Core, Native Enhancement.**
